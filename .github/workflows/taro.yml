name: taro

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - docker/taro/**

jobs:
  build:
    uses: ./.github/workflows/bake.yml
    secrets: inherit
    with:
      path: docker/taro
      target: release
