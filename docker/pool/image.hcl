# Release version
VERSION="v0.6.3-beta"

# Image config
NAME="pool"
DESCRIPTION="Lightning Pool: a non-custodial batched uniform clearing-price auction for Lightning Channel Leases (LCL)"

SKIP_VERIFY_TAG=true
